{
  "name": "rag-system",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node --env-file=.env packages/api/dist/index.js",
    "start:legacy": "node packages/api/dist/index.js"
  },
  "packageManager": "npm@10.9.2",
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.4.5",
    "@types/node": "^20.14.0",
    "vitest": "^2.0.0"
  }
}
