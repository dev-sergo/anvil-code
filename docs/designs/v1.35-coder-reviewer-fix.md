# Design — v1.35 Coder/Reviewer fix (multi-file unblock)

> **Status:** CLOSED. **Date:** 2026-05-09 → 2026-05-11.
> **Order:** после v1.34 hybrid-search. Цель — разблокировать L2 (multi-file), который v1.34 не сдвинул.
> **Links:** диагноз — [docs/benchmarks/runs/2026-05-08-l2x-multifile.md](../benchmarks/runs/2026-05-08-l2x-multifile.md).

## TL;DR

L2.x bench 2026-05-08 дал 0/8 commits и 1/8 корректного кода. Pipeline-диагностика показала: Committer **работает корректно** (его блокирует `COMMIT_ONLY_IF_VALID=true` после провала `tsc --noEmit`), а слабое звено — Reviewer (LLM-judge без AST-парса) и Coder (генерит синтаксически битый TS, либо 0 файлов). Вставляем `tsc --noEmit` **внутрь Reviewer-loop'а до LLM-judge'а** + fail-fast на 0 файлов + информативные fail-сообщения. Цель: 4+/8 корректных коммитов на повторном L2.x.

## 1. Goals & non-goals

| | |
|---|---|
| **G1** | Pre-Reviewer TS-check ловит parse-errors до LLM-judge'а; Fixer получает строку+сообщение от tsc, а не "approved" |
| **G2** | Coder с writtenFiles.length===0 после step → step помечается failed (не quiet completed) |
| **G3** | task `result` при failed содержит конкретный last review.issues / validation.errors, а не `"All N steps failed; aborting"` |
| **G4** | L2.x bench rerun: ≥4/8 валидных коммитов (Сценарий A) |
| **NG1** | Не трогаем Planner / retrieval / BM25 — диагноз показал, что они не виноваты |
| **NG2** | Не трогаем commit-gate (`COMMIT_ONLY_IF_VALID`) — он работает по дизайну |
| **NG3** | Не запускаем L3.x (5+ файлов) до закрытия G4 |
| **NG4** | Не меняем Reviewer-промпт — добавляем pre-check вокруг него, не вместо |

## 2. Архитектура

**Текущий поток (orchestrator.ts:225-262, reviewer.ts:12-38):**
```
per step:
  Coder → write files → Reviewer-LLM (3x retry с Fixer) → emit step_done
после всех шагов:
  runValidationLoop() → tsc --noEmit + tests (2x retry) → commit gate
```

**Новый поток:**
```
per step:
  Coder → write files
  if writtenFiles.length === 0: throw "step produced no changes" (G2)
  Pre-Reviewer: tsc --noEmit on changed files only (G1)
    if parse-error: feed errors → Fixer → re-run; до 2 попыток
  Reviewer-LLM (3x retry с Fixer)
после всех шагов:
  runValidationLoop() остаётся как safety net
commit gate без изменений
fail-message обогащается last issues (G3)
```

**Затронутые файлы (предполагаемо, уточнить в Phase A):**
- [packages/agents/src/orchestrator.ts](../../packages/agents/src/orchestrator.ts) — step loop, шаги 225-262, добавить pre-check между write и Reviewer
- [packages/agents/src/reviewer.ts](../../packages/agents/src/reviewer.ts) — без изменений
- [packages/safe-exec/src/type-checker.ts](../../packages/safe-exec/src/type-checker.ts) — добавить partial-check API (только изменённые файлы)
- [packages/agents/src/fixer.ts](../../packages/agents/src/fixer.ts) (если есть) — расширить input новым типом ошибок (tsc errors)
- [packages/job-system/src/worker.ts](../../packages/job-system/src/worker.ts) — обогатить fail message
- [packages/api/src/server.ts](../../packages/api/src/server.ts) — `result` в GET /task/:id уже отдаёт error если failed (line 185), но проверить что туда попадает детализация

## 3. Phases — operational checklist

> Заметки и findings заполняются по ходу. Помечать `[x]` при закрытии шага. Каждый запуск bench пишется в свой run-file.

### Phase A — Диагностика (Step 1, ~40 мин, БЕЗ правок кода)

Цель: подтвердить/опровергнуть гипотезы по L2.4/L2.6/L2.7 до начала правок.

- [x] A1. Подготовка: API запущен (backendUp=false — llama-swap <llama-swap-host>:8080 недоступен; sandbox на main, чист).
- [ ] A2. Перезапуск L2.7 (email filter) с захватом полного SSE stream — **пропущен: LLM backend down, live run невозможен**.
- [ ] A3. Перезапуск L2.4 (rename create→register) — **пропущен по той же причине**.
- [ ] A4. Перезапуск L2.6 (email уник.) — **пропущен**.
- [x] A5. Findings по code-reading (эквивалент live run):

**Findings (code-reading, 2026-05-09):**
> **L2.7 (email filter) — код ок, commit нет:** `runValidationLoop` запускает `tsc --noEmit -p tsconfig.json` на полном проекте (orchestrator.ts:725-752). Sandbox компилируется чисто на main (exit 0 подтверждено). Но генерированный код использует `request.query.email` без typed Fastify-schema → strict-mode TS2339/TS2345. Validator fails → `shouldSkipCommit=true` (orchestrator.ts:227) → commit не создаётся. Гипотеза 3 подтверждена: проблема в tsc-fail, не в commit-механизме.
> **L2.4/L2.6 (no-op):** Coder (tool-calling path) возвращает `ws.toFileChanges()` = `[]` — WorkingSet не имеет изменений (LLM только `read_file` + `done()`). `codeChanges.files.length === 0` → `currentChanges = []`. Reviewer получает пустой массив файлов → `filesSummary = ""` → LLM approves empty review. Step complete, 0 file changes. Outer step-5 write: ничего не записывает. Commit: `writtenFiles.length > 0` = false → no commit. Status=completed. Гипотеза 2 подтверждена.
> **L2.2/L2.5 (false-positive):** Reviewer — чистый LLM-judge (reviewer.ts:12-38), видит FileChange как текст. Не запускает tsc. Синтаксически битый код (метод врезан в середину другого) может выглядеть логично в диффе → Reviewer approves. Validation fails. `shouldSkipCommit=true`. Гипотеза 1 подтверждена.

### Phase B — Pre-Reviewer TS-check (Step 2, ~1-2 дня)

Главный архитектурный фикс. Ожидаемая отдача: убирает класс L2.2/L2.5 false-positives.

- [x] B1. Расширить `TypeChecker` partial API: `runOn(paths: string[]): Promise<ValidationResult>` — запускает полный `tsc --noEmit -p tsconfig.json`, фильтрует output к строкам изменённых файлов. Если ошибок в наших файлах нет — возвращает `success:true` (не блокируем на чужих pre-existing errors).
- [x] B2. В `orchestrator.ts:executeStep` добавлен `applyAndCheckTs()` + pre-Reviewer блок (до 2 попыток Fixer): временно пишет файлы на диск, запускает `typeChecker.runOn()`, восстанавливает оригиналы. Работает для tool-calling (create-action) и patch-based (modify-action) Coder output.
- [x] B3. Fixer prompt уже содержит TypeScript-specific patterns (TS2304, TS2362, etc.) — отдельный input блок не потребовался; tsc errors передаются как `issues: ["TypeScript compilation failed:\n<output>"]`.
- [x] B4. Юнит-тесты в `type-checker.test.ts`: skipped/clean/failure/errors-in-other-file — все mock-based (изолируют логику фильтрации от tsc availability).
- [x] B5. Pre-check фильтрует только `.ts`/`.tsx` без `delete` action — JSON/MD/YAML проходят без проверки.

**Open questions:**
> B1: Решили запускать full-project tsc (не subset файлов) — корректнее для cross-file dependencies. Фильтрация в runOn гарантирует что чужие ошибки не блокируют наш step. Wall-time на sandbox (6 файлов) — секунды, приемлемо.

### Phase C — Fail-fast + информативные сообщения (Step 3, ~полдня)

- [x] C1. В `orchestrator.ts:executeStep` после `currentChanges = [...codeChanges.files]`: если length === 0 — throw с "Coder produced no changes". Раньше шаги тихо завершались с 0 изменениями (L2.4/L2.6).
- [x] C2. Перед throw'ом эмитится `step_noop` event в taskEvents — SSE stream покажет его клиентам.
- [x] C3. В `executePlanParallel` добавлен `stepFailures: Map<string, string>` — catch-блок сохраняет `msg` по stepId. В `runTask`: "All N steps failed" заменено на "All N steps failed — Step s1: <reason>; Step s2: <reason>". worker.ts не трогали — ошибка пробрасывается из runTask как throw.
- [x] C4. GET /task/:id уже возвращает `error` в server.ts:185 — новый формат корректно попадёт туда через worker.ts → store.updateTaskStatus(id, 'failed', message).
- [ ] C5. Юнит-тест — пропущен (scope этой сессии ограничен); покрыт косвенно через B4 + live rerun D2.

### Phase D — Rerun L2.x bench (Step 4, ~45 мин)

- [x] D1. Sandbox cleanup до main.
- [x] D2. Прогнаны все 8 задач L2.1–L2.8. Run-file: [docs/benchmarks/runs/2026-05-10-v1.35-l2x-rerun.md](../benchmarks/runs/2026-05-10-v1.35-l2x-rerun.md).
- [x] D3. Таблица заполнена.
- [x] D4. Сравнение с baseline:
  - false-positives: 2→0 ✅ (L2.5 теперь правильный fail, pre-check поймал TS2741)
  - no-op'ы: 2→0 ✅ (G2 fail-fast работает; L2.6 стал корректным коммитом)
  - Новых корректных коммитов: 0→2 (L2.2, L2.6); общих коммитов: 0→3
  - L2.7 — регрессия по симптому: context window exceeded (36k tokens)
- [x] D5. Verdict: **Сценарий B** — 2/8 корректных коммитов (AC4 = ≥4/8 не закрыт).

**Run results:**
| # | Verdict |
|---|---|
| L2.1 | ❌ LLM JSON parse |
| L2.2 | ✅ commit, корректный код |
| L2.3 | ❌ Reviewer 3× rejected |
| L2.4 | ⚠️ commit, неверный код |
| L2.5 | ✅ pre-check TS2741 (правильный fail, был false-positive) |
| L2.6 | ✅ commit, корректный код |
| L2.7 | ❌ context overflow 36k tok |
| L2.8 | ✅ pre-check TS2307 (правильный fail) |

## 4. Acceptance criteria

- [x] **AC1** — Pre-Reviewer TS-check ловит parse-errors L2.5-class: L2.5 TS2741, L2.8 TS2307 пойманы до LLM-judge'а. Unit-тесты (4 случая) зелёные.
- [x] **AC2** — Coder с 0 файлов даёт `failed` + `step_noop` event (C1/C2 реализованы). L2.4/L2.6 в baseline были no-op — теперь либо fail-fast, либо реальные изменения.
- [x] **AC3** — Failed result содержит `"Step step1: TS pre-check failed after fix retry — src/..."` — конкретный диагноз, длиннее 50 символов.
- [ ] **AC4** — L2.x rerun: 2/8 корректных коммитов (L2.2 + L2.6). Цель ≥4/8 **не закрыта**. Новые блокеры: context overflow L2.7, Reviewer reject L2.3, LLM JSON parse L2.1.
- [x] **AC5** — Wall-time: median ~3 мин/задача (как в baseline). Pre-check на 2-3 файла — секунды, не влияет на total. L2.7 упала по другой причине (context, не pre-check).

## 5. Risks & mitigations

| Risk | Severity | Mitigation |
|---|---|---|
| `tsc --noEmit` на изменённых файлах падает из-за зависимостей с другими файлами проекта | MED | Запускать с `--project` указывая sandbox tsconfig, не отдельные файлы; tsc разрешит импорты сам |
| Pre-check добавит секунды × 3 retry на каждый step → wall-time +10-30% | MED | Запускать после первого write; кэшировать tsc через incremental build (`--incremental`) |
| Fixer не справится с tsc-errors (промпт-формат не подходит) | MED | B3 — отдельный test cycle, в случае провала — fallback к LLM-judge'у с tsc-output как контекст |
| Pre-check ловит false-positive (валидный код, но tsc warning) | LOW | Различать `error` vs `warning` в TscResult; reject только на errors |

## 6. Open questions

- Pre-check работает на полный `tsc --noEmit -p sandbox/tsconfig.json` или на список файлов? Проверить performance в Phase B1.
- Стоит ли добавлять `eslint --no-eslintrc --parser @typescript-eslint/parser` как промежуточный (быстрее tsc)? — оставлено на v1.36 если pre-check окажется медленным.
- L2.7 — нужно сначала понять причину провала (Phase A2), может потребоваться отдельный фикс вне scope этой итерации.

## 7. Alternatives considered

- **Заменить LLM-judge Reviewer на pure tsc + tests.** Отвергнуто: LLM-judge ловит семантические проблемы (плохая архитектура, нарушение контракта), которые AST не видит. Берём оба.
- **Добавить tsc в Coder loop, не в Reviewer.** Отвергнуто: Coder уже большой prompt; чище отделить генерацию от валидации.
- **Использовать TS Compiler API программно вместо spawn'а tsc.** Отвергнуто на эту итерацию: больше кода, +зависимость; spawn простой и работает в текущем `safe-exec`.

## 8. Out of scope (для следующих итераций)

- v1.36 — L3.x (5+ файлов): после AC4 закрыто.
- v1.37 — vscode-extension UX для отображения step_noop / tsc-errors.
- Повторить bench L4.1 (single-file) после v1.35 чтобы убедиться что не сломали — но это smoke, не итерация.

## 9. Notes (free-form, для агента следующей сессии)

> Заполнять открытыми вопросами, surprise findings, отклонениями от плана. Не удалять старое.

**2026-05-09 — Phase A-C закрыты, Phase D (rerun) ждёт backend:**

- LLM backend (llama-swap <llama-swap-host>:8080) был недоступен во время сессии. Phase A2-A4 (live rerun для диагностики) заменены code-reading анализом — гипотезы подтверждены по коду.
- `applyAndCheckTs()` — временно пишет файлы на диск через SafeWriter, запускает tsc, восстанавливает оригиналы в `finally`. Backup-manager SafeWriter'а при этом создаёт копии — это OK (бэкапы в бэкапах-директории, не в проекте).
- B4 тесты: изначально пытались использовать реальный tsc в tmpDir (за пределами rag-system) — tsc возвращал success:true для broken.ts. Переключились на mock-based тесты (`vi.spyOn(checker, 'run')`), которые надёжно тестируют filtering-логику runOn.
- B3: Fixer prompt уже содержит TS-specific patterns. issues-формат `["TypeScript compilation failed:\n<output>"]` совпадает с `runValidationLoop` (orchestrator.ts:731-732). Единообразие без доп. изменений.
- C3: stepFailures Map добавлен в executePlanParallel. Деструктуризация в runTask обновлена. worker.ts не трогали — он уже корректно проксирует throw из runTask в store.updateTaskStatus.
- Phase D выполнена 2026-05-10: 2/8 корректных коммитов, AC4 не закрыт.

**2026-05-11 — v1.36-a выполнен, итерация закрыта:**

- pruneHistory: false→true в FEATURE_SPEC — устранил context overflow (L2.7 больше не падает на 36k tokens, теперь LLM JSON parse — model-level).
- .js suffix: TS2307 ушёл из L2.8. Но TS2339 (`request.startTime`) устойчивый — Fixer меняет имя поля но не паттерн. Coder стабильно генерирует `request.X = Date.now()` вместо `reply.elapsedTime`. Prompt-hints не помогают если Coder изначально неверен.
- L2.7/L2.8 — model-quality проблемы. Prompt engineering на Fixer-уровне исчерпан.
- **Стратегия на следующую итерацию:** Planner-декомпозиция сложных задач (L2.8 на 2 шага), либо проверить другую модель для Coder роли.
