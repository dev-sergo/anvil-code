# v1.68 — Bench repair: correct T2/T5/H4 tasks

## Problem

v1.67 bench scored 9/12 (75%). Three failures (T2, T5, H4) were caused by a **benchmark design flaw**: the tasks asked to implement features that already existed in the target repos.

| Task | What we asked | Reality |
|---|---|---|
| T2 `getHTTPStatusCode` | Add HTTP-status helper | `getHTTPStatusCode` + `getHTTPStatusCodeFromError` already in `unstable-core-do-not-import/http/getHTTPStatusCode.ts`, publicly exported |
| T5 `maxBodySize` | Add body-size limit returning 413 | `maxBodySize?: number` already in `NodeHTTPHandlerOptions`; fully enforced in `incomingMessageToRequest.ts` |
| H4 `requestId middleware` | Create requestId middleware | `src/middleware/request-id/` already exists with `index.ts`, `request-id.ts`, `index.test.ts` |

Root cause: tasks were written from memory/imagination without grepping the repos first.

## Goal

Replace T2/T5/H4 with tasks for features **verified to be absent**. Re-run only those three. Target: ≥ 11/12.

## Replacement tasks

### T2-new — `requestTimeout` option in `nodeHTTPRequestHandler`

**Verified absent**: grep for `requestTimeout|408|TIMEOUT` in `packages/server/src/adapters/node-http/` → no results.

Add `requestTimeout?: number` (milliseconds) to `NodeHTTPHandlerOptions`. When provided, start an `AbortController` timer on request entry; if the timeout fires before the response finishes, send `HTTP 408` with a TIMEOUT TRPCError body and abort. Add JSDoc. Write a vitest unit test that sends a slow request (fake timers) and asserts 408.

Files touched:
- `packages/server/src/adapters/node-http/types.ts` — add `requestTimeout?: number`
- `packages/server/src/adapters/node-http/nodeHTTPRequestHandler.ts` — wire timeout
- `packages/server/src/adapters/node-http/__tests__/requestTimeout.test.ts` — new

### T5-new — `getConnectionCount()` on `createHTTPServer`

**Verified absent**: grep for `getConnectionCount|activeConnections|connectionCount` in `packages/server/src/` → no results.

Add a `getConnectionCount(): number` method on the object returned by `createHTTPServer`. Track active connections using `server.on('connection', ...)` / `socket.on('close', ...)`. Add JSDoc. Write a vitest unit test that opens N connections to the test server and asserts `getConnectionCount() === N`, then closes them and asserts 0.

Files touched:
- `packages/server/src/adapters/standalone.ts` — extend return type + impl
- `packages/server/src/adapters/__tests__/getConnectionCount.test.ts` — new

### H4-new — `responseTime` middleware

**Verified absent**: grep for `X-Response-Time|response-time|responseTime` in `src/middleware/` → no results. Existing `timing` middleware uses `Server-Timing` API (performance metrics), not `X-Response-Time`.

Create `src/middleware/response-time/index.ts`. Middleware records `Date.now()` before calling `next()`, then appends `X-Response-Time: <ms>ms` response header. Accept optional `headerName` (default `'X-Response-Time'`) and `suffix` (default `'ms'`, pass `false` to omit). Add JSDoc. Export from `src/middleware/index.ts`. Write vitest unit tests in `src/middleware/response-time/index.test.ts`.

Files touched:
- `src/middleware/response-time/index.ts` — new
- `src/middleware/index.ts` — re-export
- `src/middleware/response-time/index.test.ts` — new

## Process improvement

Before writing any future bench task:
1. `grep -rn "<feature-name>" <repo>/src/` — confirm absent
2. If found → pick different feature or reframe as "add test coverage for X"
