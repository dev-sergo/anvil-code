# Run 2026-05-29 — v1.70 Cross-project pattern bench

## Configuration

| | |
|---|---|
| Date | 2026-05-29 |
| rag-system revision | `019fe02` + v1.69 changes |
| Iteration tag | v1.70 — cross-project repo memory bench |
| LLM_LARGE_MODEL | `qwen3-32k` |
| LLM_SMALL_MODEL | `qwen3-32k` |
| LLM_BACKEND | llamacpp @ 172.20.10.4:8080 |
| VECTOR_BACKEND | qdrant (http://localhost:6333) |
| RAG_MAX_CONTEXT_TOKENS | 1500 |
| RAG_GRAPH_HOPS | 3 |
| TOOL_CALLING_CODER | true |
| TESTER_ENABLED | true |
| trpc rev | `23c723c` (v11.17.0) |
| hono rev | `f10dee8` (4.12.18) |
| Baseline | v1.68c: 12/12 (100%) |
| Cumulative? | no (each task on clean main) |

**Goal:** Verify that v1.69 cross-project patterns actually surface during tasks. hono patterns → trpc context, trpc patterns → hono context.

---

## tRPC Results (T1–T6)

| # | Task | Result | Commit | Notes |
|---|------|--------|--------|-------|
| T1 | JSDoc TRPCError constructor | ✅ | `b7d389b` | Clean pass |
| T2 | requestTimeout in nodeHTTPRequestHandler | ❌ | — | 1a17511d completed, no commit; validation failed |
| T3 | onError callback in standalone | ❌ | — | 78f5d0bf no commit; e2006a2b context overflow |
| T4 | createTimeout AbortSignal utility | ✅ | `7c27cf4` | Clean pass |
| T5 | getConnectionCount on createHTTPServer | ✅ | `0acae62` | Clean pass |
| T6 | dataLoader retry option | ❌ | — | 480dc6a1 completed, no commit; validation failed |

**tRPC v1.70: 3/6** (T1✅ T2❌ T3❌ T4✅ T5✅ T6❌)

---

## Hono Results (H1–H6)

| # | Task | Result | Commit | Notes |
|---|------|--------|--------|-------|
| H1 | getUserAgent helper | ✅ | `be26256` | ef6d449d; impl + export |
| H2 | getRequestId helper | ✅ | `0ec4024` | c1aaefb3; impl + test + export |
| H3 | parseQueryString utility | ✅ | `1a5e463` | aab514b5; impl + test + export |
| H4 | responseTime middleware | ✅ | `e193921` | 585143d2; impl + test + export |
| H5 | getHeader helper | ✅ | `47fa483` | 8b591460; impl + test + export |
| H6 | buildUrl utility | ❌ | `70c6094`* | 5084cbe9; test-only commit, impl missing |

\* 70c6094 exists but only contains `build-url.test.ts` — implementation was written to disk but not included in `git.add(files)`.

**Hono v1.70: 5/6** (H1✅ H2✅ H3✅ H4✅ H5✅ H6❌)

---

## Summary

| | v1.68c | v1.70 |
|---|---|---|
| trpc | 6/6 (100%) | 3/6 (50%) |
| hono | 6/6 (100%) | 5/6 (83%) |
| **Total** | **12/12 (100%)** | **8/12 (67%)** |

---

## Failure analysis

### T2 (requestTimeout) — validation failed
- Coder generated code; validation failed; commit skipped
- Previous successful run (268a0a2e → ae645ab) was the v1.68c session, not counted here
- Known instability: p(success) ≈ 25% per attempt

### T3 (onError standalone) — context overflow
- First attempt (e2006a2b): `Context size has been exceeded` from LLM
- Second attempt (78f5d0bf): completed but no commit (validation failed)
- Context overflow on T3 is a known recurring issue (large standalone.ts file)

### T6 (dataLoader retry) — validation failed
- 480dc6a1 completed with no commit
- Fixer failed to get tests passing within MAX_TOOL_CALLS

### H6 (buildUrl) — test-only commit bug
- Coder wrote `build-url.ts` + `build-url.test.ts`; both existed on disk during validation (tests passed)
- `git.add(files)` received file list containing only the test file — implementation excluded
- After branch switch, `git clean -fd` removed the untracked `build-url.ts`
- **Root cause:** SafeWriter returns incomplete file list when Coder output is partial
- **Fix needed:** orchestrator should diff branch vs HEAD and add all modified/untracked files, not rely on Coder's stated file list

---

## Cross-project pattern evaluation

v1.69 cross-project patterns were active during this bench. Effect on results:
- hono scored 5/6 (same as v1.68b, down from v1.68c 6/6 — H6 was a commit bug, not a logic failure)
- trpc scored 3/6 (regression from 6/6; pattern injection did not prevent T2/T3/T6 failures)
- The dominant failure mode this run was model variance + context overflow, not pattern mismatch
- Cross-project signal: inconclusive — regression is within normal variance for these tasks

---

## v1.71 action items

1. **Fix H6 commit bug:** Orchestrator should use `git diff --name-only HEAD` after writing files and commit everything changed, not rely on Coder's stated file list (or validate that the file list matches what was written)
2. **T3 context overflow:** Reduce RAG snippets for large-file tasks, or add a context-budget guard before calling the LLM
3. **T2/T6 reliability:** Increase MAX_TOOL_CALLS for Fixer (30 → 50) or add a second Fixer pass with a fresh prompt
