# Run 2026-05-17 — Vite Cross-Repo Bench, qwen2.5-coder Coder (v1.59)

## Configuration

| | |
|---|---|
| Date | 2026-05-17 |
| rag-system revision | `91378ae` (v1.59) |
| Mode | balanced |
| **LLM_LARGE_MODEL** | `qwen2-5-coder-32b-instruct-q4-k-m-ctx-32k-q4-0-kv-t02` (switched from Gemma) |
| LLM_SMALL_MODEL | `qwen2-5-coder-32b-instruct-q4-k-m-ctx-32k-q4-0-kv-t02` |
| Backend | llama-swap @ 172.20.10.4:8080 |

## Results

| Task | Result | Note |
|------|--------|------|
| V1 — JSDoc on defineConfig (config.ts 2728 lines) | ❌ Reviewer: "JSDoc missing" | Coder produced empty diff — file too large |
| V2 — getViteVersion new file | ❌ eslint: unused `resolve` import | Code correct, import cleanup needed |
| V3 — parseAcceptHeader in utils.ts | ✅ **committed** | Was Gemma model-ceiling. 12 lines, clean impl |
| V4 — requestLogger middleware | ❌ Reviewer: wrong implementation | Large file navigation issue remains |
| V5 — HMR_HEADER_NAME constant | ✅ **committed** | ESM guard filtered require() spec file |
| V6 — createServer JSDoc | ✅ **committed** | Was Gemma noop — navigated re-export chain |

**3/6 commits** — V3, V5, V6. Same count as Gemma best (3/6: V1,V2,V5) but different tasks.

## V3 diff (parseAcceptHeader, 12 lines)

```diff
+export function parseAcceptHeader(header: string): string[] {
+  const mediaTypes = header.split(',').map((part) => {
+    const [type, qParam] = part.trim().split(';')
+    const qValue = qParam ? parseFloat(qParam.split('=')[1]) : 1.0
+    return { type, qValue }
+  })
+  mediaTypes.sort((a, b) => b.qValue - a.qValue)
+  return mediaTypes.map(({ type }) => type)
+}
```

Added at end of utils.ts (1835 lines) via `add_export`. qwen2.5-coder correctly used the tool.

## V6 diff (createServer JSDoc, 5 lines)

```diff
+/**
+ * Starts a Vite dev server.
+ * @param inlineConfig - Optional inline configuration for the server.
+ * @returns A promise that resolves to the ViteDevServer instance.
+ */
 export function createServer(
```

Found the actual definition through the re-export chain — Gemma always nooped here.

## Model comparison

| Task | Gemma-26B (isolated) | qwen2.5-coder-32B |
|------|---------------------|-------------------|
| V1 JSDoc (config.ts 2728 lines) | ✅ sometimes | ❌ empty diff |
| V2 new file | ✅ sometimes | ❌ eslint (unused import) |
| V3 parseAcceptHeader | ❌ ceiling | ✅ |
| V4 middleware | ❌ | ❌ |
| V5 constant | ✅ | ✅ |
| V6 JSDoc (re-export) | ❌ noop | ✅ |

**Key finding:** qwen2.5-coder is better at code navigation (V3, V6) but worse at very large files (V1 2728 lines → empty diff). Gemma managed V1 sometimes due to different token sampling. Neither model reliably handles files >1500 lines.

## v1.59 fixes validated

- **ESM guard** (`validateAndFilterTestFiles`): discards test files with `require()`/`__dirname` in ESM projects. V5 now commits cleanly (TesterAgent spec with `require()` filtered).
- **qwen2.5-coder** as Coder: reliable tool-calling on moderate files (<1000 lines), noop on 2728-line config.ts.
