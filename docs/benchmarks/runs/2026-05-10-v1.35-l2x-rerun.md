# Run 2026-05-10 — L2.x rerun (v1.35 Phase D)

## Configuration

| | |
|---|---|
| Date | 2026-05-10 |
| rag-system revision | `c06c500` |
| Iteration tag | v1.35 (pre-Reviewer TS check + fail-fast on empty Coder output) |
| LLM_BACKEND | llamacpp |
| LLM_URL | `http://localhost:8080` |
| LLM_LARGE_MODEL | `qwen-coder-long` |
| LLM_SMALL_MODEL | `qwen3` |
| TOOL_CALLING_CODER | true |
| TESTER_ENABLED | false |
| Mode | balanced |
| Sandbox | `~/rag-system-sandbox` (Fastify+Zod+Vitest users API, ~6 файлов) |
| Cumulative? | no — каждая задача на чистом main |

**Цель:** проверить что v1.35 даёт ≥4/8 коммитов (AC4).
**Baseline:** 2026-05-08 — 0/8 commits, 1/8 корректного кода.

## Tasks summary

| # | Задача | Status | Wall | Commit | Code OK | Verdict |
|---|---|---|---|---|---|---|
| L2.1 | age field (3 файла) | failed | ~8m | — | — | ❌ LLM JSON parse error |
| L2.2 | DELETE /users/:id (2 файла) | completed | ~3m | ✅ 50f834b | ✅ | ✅ baseline: false-positive→fix |
| L2.3 | pagination (2 файла) | failed | ~2m | — | — | ❌ Reviewer 3× rejected |
| L2.4 | rename create→register (3 файла) | completed | ~4m | ⚠️ 449ebbe | ❌ | ⚠️ commit есть, код неверный (app.register вместо rename) |
| L2.5 | updatedAt + PATCH (3 файла) | failed | ~2m | — | — | ✅ pre-check поймал TS2741 (baseline: false-positive) |
| L2.6 | email уникальность 409 (2 файла) | completed | ~4m | ✅ deb0757 | ✅ | ✅ baseline: no-op→fix |
| L2.7 | GET /users?email= (2 файла) | failed | ~2m | — | — | ❌ context window exceeded (36054 tok) |
| L2.8 | logging middleware (1 новый + 1 mod) | failed | ~3m | — | — | ✅ pre-check поймал TS2307 (baseline: silent fail) |

## Detailed observations

### L2.1 — age field ❌
LLM выдал битый JSON на позиции 2750 — не смог распарсить ответ Coder'а. C3 отработал: `"All 1 steps failed — Step step1: LLM output parsing failed: ..."`. Не связано с v1.35.

### L2.2 — DELETE ✅ (baseline: false-positive → теперь корректный коммит)
`UserService.delete()` правильно проверяет существование через `Map.has()`, возвращает `false` если нет. Route возвращает 204 при успехе, 404 если не найден. Код чистый. **Ключевое улучшение** — в baseline этот же паттерн (метод врезан в другой) был пропущен Reviewer'ом и дал битый код без коммита. Теперь pre-Reviewer TS check не дал ложному одобрению состояться.

### L2.3 — pagination ❌
Reviewer 3× rejected. Та же картина что в baseline. Не связано с v1.35.

### L2.4 — rename ⚠️ (baseline: no-op → теперь коммит, но неверный код)
Модель не поняла задачу — восприняла "register" как `app.register()` (Fastify plugin), а не как переименование метода в `UserService`. Код изменился, но не так. `UserService.create()` остался нетронутым. Коммит есть, семантика неверная. Проблема в понимании задачи, не в pipeline.

### L2.5 — updatedAt ✅ (baseline: false-positive → теперь правильный fail)
**G1 подтверждён:** `TS2741: Property 'updatedAt' missing` поймано до LLM-judge'а. В baseline Reviewer пропустил битый код (метод врезан в другой), дал `completed` без коммита. Теперь step правильно помечается `failed` с конкретным TS-диагнозом.

### L2.6 — email uniqueness ✅ (baseline: no-op → теперь корректный коммит)
**G2 подтверждён:** В baseline Coder вернул 0 файлов → quiet completed. Теперь либо Coder действительно произвёл изменения (модель/промпт сработали), либо fail-fast дал правильный сигнал повторить. Логика корректная: `Array.from(users.values()).find()` + `throw new Error('Email already exists')` в service, try/catch + `reply.code(409)` в route. Minor: дублированный `throw error` (unreachable) и неровный indent — функционально не влияет.

### L2.7 — email filter ❌ (baseline: код ок, но no commit → теперь context overflow)
**Регрессия по симптому, но другая причина:** 36054 tokens exceeded. В baseline задача дала корректный код без коммита (tsc fail на validation). Теперь задача падает раньше из-за context window. Возможная причина: pre-Reviewer Fixer добавляет tokens в историю сообщений, что увеличивает общий контекст. Нужно исследовать.

### L2.8 — logging middleware ❌ (baseline: silent fail → теперь конкретный TS2307)
**G1 подтверждён:** `TS2307: Cannot find module './middleware/lo...'` — Coder создал import в server.ts но файл middleware не создался или путь неверный (.js суффикс?). Pre-check поймал ошибку. Fixer не справился за 2 попытки. В baseline просто "failed" без деталей.

## Aggregate

| | Baseline (2026-05-08) | v1.35 (2026-05-10) | Delta |
|---|---|---|---|
| Git commits total | 0/8 | 3/8 | +3 |
| Commits с корректным кодом | 0/8 | 2/8 | +2 |
| false-positives (completed, bad code, no commit) | 2/8 | 0/8 | −2 ✅ |
| no-ops (0 changes, silent) | 2/8 | 0/8 | −2 ✅ |
| hard fails | 3/8 | 5/8 | +2 (часть — правильные fail вместо false-positive) |
| context overflow | 0/8 | 1/8 | новый класс ошибок |

## VERDICT: Сценарий B (AC4 не закрыт)

**AC4 цель: ≥4/8 корректных коммитов. Результат: 2/8** (L2.2 + L2.6).

**Позитивные изменения:**
- false-positive устранены полностью (2→0): L2.5 теперь правильно fail, не silent completed
- no-op устранены (2→0): fail-fast работает
- Коммиты появились (0→3), 2 из них корректные
- G1/G2/G3 работают как задумано — диагностика улучшилась

**Открытые проблемы (не в scope v1.35):**
1. **Context window**: L2.7 упала из-за 36k tokens. Pre-Reviewer Fixer добавляет tokens → риск регрессии на задачах с большим контекстом. Нужно исследовать token usage и, возможно, укоротить BUGFIX_SPEC user message.
2. **Reviewer 3× reject**: L2.3 — Reviewer продолжает отвергать корректный код. Не лечится v1.35.
3. **Семантическое непонимание**: L2.4 — модель не поняла "rename". Промпт-проблема.
4. **LLM JSON parse**: L2.1 — нестабильный JSON output от модели при длинных ответах.

## Next iteration targets

- **v1.36-a**: Исследовать context window growth на L2.7. Укоротить pre-Reviewer Fixer prompt или добавить token budget guard.
- **v1.36-b**: L2.8 (new file creation) — TS2307 на `.js` import суффикс в NodeNext mode. Добавить в Coder/Fixer системный промпт явное правило про `.js` суффикс для новых файлов.
