# Run 2026-05-15 — v1.44 Cumulative Bench (TesterAgent dry-run)

## Configuration

| | |
|---|---|
| Date | 2026-05-15 |
| rag-system revision | `d032844` (v1.44) |
| CUMULATIVE_MODE | true |
| CUMULATIVE_BRANCH | auto/cumulative |
| Project | rag-system-sandbox (ID `917e347a4b0f`) |
| v1.44 change | TestRunner.runOn + dry-run in validateAndFilterTestFiles |

Two sequential runs (same 6 tasks, fresh sandbox each time).

---

## Results

| Task | Run 1 | Run 2 | Notes |
|------|-------|-------|-------|
| C1 DELETE /users/:id | ✅ 140s | ✅ 212s | stable |
| C2 email filter | ✅ 59s | ✅ 273s | stable |
| C3 updatedAt + PATCH | ✅ 136s | ✅ 136s | stable |
| C4 pagination (limit/offset) | ❌ ts_fail 305s | ✅ 142s | flaky 1/2 |
| C5 TTL sessionExpiry + POST /session | ❌ test_fail 408s | ❌ reviewer_reject 271s | unstable |
| C6 rate limiting in server.ts | **✅ 111s** | **✅ 58s** | **was ❌ in v1.43** |

**Run 1: 4/6 | Run 2: 5/6**

---

## v1.44 dry-run effect on C6

v1.43: C6 rate limit test failed — TesterAgent wrote `server.test.ts` with timing-sensitive rate limit assertions + empty `afterEach` (couldn't use `UserService.delete` since no delete method then). Test ran in validation and failed → commit_skipped.

v1.44: `validateAndFilterTestFiles` runs `testRunner.runOn(newTestPaths)` after TS check. Server test file fails the dry run → discarded. Coder's `server.ts` rate limiting implementation committed cleanly. **C6 ✅ in both runs.** Dry-run confirmed effective.

---

## C4 / C5 analysis

**C4 pagination (flaky):**
- Run 1 fail: TypeScript errors — Coder changed `GET /users` return type to `{users,total}` but something in the call chain had TS errors.
- Run 2 pass: Different Coder path, correct types throughout.
- Root: multi-type-change task (response format + types) is sensitive to Coder's navigation order.

**C5 TTL session (unstable):**
- Run 1 fail: Tests failed (Tester wrote wrong assertions, dry-run didn't catch them because tests ran OK initially then failed in validation with full suite).
- Run 2 fail: Reviewer reject — "Missing implementation in src/types.ts". Coder added the endpoint but forgot to add `sessionExpiry?: string` to User interface. Multi-file task gap.
- Root: C5 requires edits to both `src/types.ts` AND `src/routes/users.ts`. Planner sometimes creates 1 step that covers both; Coder sometimes misses one file.

---

## Comparison

| | v1.37 (manual) | v1.43 (auto) | v1.44 (dry-run) |
|--|-------|-------|-------|
| C1 | ✅ | ✅ | ✅ |
| C2 | ✅ | ✅ | ✅ |
| C3 | ✅ | ✅ | ✅ |
| C4 | ✅ (race) | ❌ test_fail | 1/2 (flaky) |
| C5 | ✅ | ✅ | ❌ |
| C6 | ❌ Reviewer | ❌ test_fail | **✅** |
| Score | 5/6 | 5/6 | **4.5/6 avg** |

C6 definitively closed by dry-run. C4/C5 instability is model/context-dependent (Gemma 26B multi-file).
