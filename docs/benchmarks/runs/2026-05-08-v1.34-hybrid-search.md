# Bench Run: v1.34 Hybrid Search (BM25 + Dense RRF)
**Date:** 2026-05-08  
**Branch:** dev  
**Models:** qwen-coder-long (LLM_LARGE), qwen3 (LLM_SMALL), embed (embeddings), reranker  
**Backend:** llama-swap @ localhost:8080  
**Project:** rag-system-sandbox (5 TS files, ~150 LOC)

---

## Summary

| Test  | Pass/Total | Avg Wall  | Notes                                  |
|-------|-----------|-----------|----------------------------------------|
| L1.1  | 3/3       | ~77s      | `/health` endpoint, retrieval correct  |
| L4.1  | 2/3       | ~157s     | bug fixed, no test files created (✓ interceptToolCall) |

---

## L1.1 — Add GET /health (regression guard)
**Task:** "Add a GET /health endpoint that returns {status: 'ok'}."  
**Baseline:** sandbox @ `24ce9fa` (initial skeleton, no health endpoint)

| Run | Status    | Wall  | Retrieved files                              | Result           |
|-----|-----------|-------|----------------------------------------------|------------------|
| r1  | completed | 70s   | users.ts, user-service.ts, types.ts          | Health route added to usersRoutes ✓ |
| r2  | completed | 70s   | users.ts, user-service.ts, types.ts          | Health route added ✓ |
| r3  | completed | 90s   | users.ts, user-service.ts, types.ts          | Health route added ✓ |

**Notes:**
- All 3 runs added the `/health` route correctly inside `usersRoutes` (registered at root — correct)
- Retrieval: `server.ts` was NOT retrieved but the task still succeeded (health route added to plugin)
- BM25 hybrid search not decisive here since files are small and HNSW already finds them

---

## L4.1 — Fix missing createdAt (Fixer interceptToolCall guard)
**Task:** "The POST /users endpoint returns users without a createdAt field. The User interface requires createdAt as a string. Find and fix the bug."  
**Baseline:** sandbox @ `bench-l41-baseline` (UserService.create uses `} as User;` — missing createdAt)  
**Bug branch:** `bench-l41-baseline` branching from `24ce9fa`

> **Infrastructure fix required:** `git-engine/src/engine.ts` always checked out `main` instead of `config.git.defaultBranch`.  
> Fixed in this session — GIT_DEFAULT_BRANCH=bench-l41-baseline used for these runs.

| Run | Status    | Wall  | No test files | createdAt fixed | Committed |
|-----|-----------|-------|---------------|-----------------|-----------|
| r1  | completed | 130s  | ✓             | ✗ (validation loop failed) | ✗ |
| r2  | completed | 80s   | ✓             | ✓               | ✓ |
| r3  | completed | 261s  | ✓             | ✓               | ✓ |

**interceptToolCall behavior:**
- r1: Fixer triggered 3 times (validation loop), each attempt wrote `user-service.ts` (not test files) — interceptToolCall not needed, Fixer chose production code correctly
- r2, r3: Fix committed successfully
- No untracked test files in any run ✓

**Validation failures (r1):**
- Tests in `tests/users.test.ts` (from initial skeleton) check `expect(user.createdAt).toBeTruthy()`
- Coder made incomplete fix (type annotation only, no `createdAt` value), Reviewer approved, validation failed
- Fixer made 3 attempts but couldn't resolve → COMMIT_ONLY_IF_VALID=true skipped commit

---

## Infrastructure Issues Found & Fixed

| Issue | Fix | Status |
|-------|-----|--------|
| `git-engine` always branched from `main` | Added `config.git.defaultBranch` support | ✓ committed |
| Bench script used wrong API field names | Fixed `description→task`, `projectRoot→project`, `done→completed` | ✓ committed |
| Qwen3 thinking mode returns empty content | `chat_template_kwargs: {enable_thinking: false}` | ✓ committed |

---

## Retrieval Quality (v1.34 Hybrid BM25+Dense)

L1.1 retrieval: `[users.ts, user-service.ts, types.ts]` — `server.ts` missing but task succeeded.  
L4.1 retrieval: `[types.ts, user-service.ts, routes/users.ts]` — correct files for the bug.

BM25 added keyword-level matching. Precision@3 on sandbox = 3/3 relevant files for L4.1.

---

## Next Steps
- L2.1/L2.2 on target: need to adapt task descriptions to match target codebase structure
- L1.1 retrieval: `server.ts` should be retrieved (BM25 should match "health endpoint server")
- L4.1 r1 Fixer failure: model not reliably adding `createdAt` value — may need prompt improvement
