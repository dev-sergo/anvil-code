# Run 2026-05-14 — v1.40 TesterAgent validation regression bench

## Configuration

| | |
|---|---|
| Date | 2026-05-14 |
| rag-system revision | `2e54503` (v1.40 content guard) |
| LLM_LARGE_MODEL | `gemma` (gemma-4-26b-a4b-it-mxfp4-moe-ctx-32k) |
| LLM_BACKEND | llamacpp @ 172.20.10.4:8080 |
| RAG_MAX_CONTEXT_TOKENS | 1500 |
| TOOL_CALLING_CODER | true |
| TESTER_ENABLED | true |
| Mode | balanced |

**v1.40 changes under test:**
- `validateAndFilterTestFiles()`: TS check on generated test files, discard on error
- Content guard: discard files with no `it()/test()` calls (Rule 9 — empty describe)
- TesterAgent prompt rules 11–12: variable declaration, fragile list assertions

---

## L1.1 — Add /health endpoint (×3)

| Run | Result | Time | Notes |
|-----|--------|------|-------|
| r1 | ✅ commit | 53s | 2 files (route + tester tests valid). |
| r2 | ✅ commit | 56s | git.lock race (prev task still running) — task self-recovered via own checkout. 2 files. |
| r3 | ✅ commit | 62s | Clean. |

**L1.1: 3/3** ✅ — fixed from v1.39 2/3. `body is not defined` pattern no longer reaches validation.

---

## L4.1 — Find and fix missing createdAt (×3 + 1 bonus)

| Run | Result | Time | Notes |
|-----|--------|------|-------|
| r1 | ❌ commit_skipped | 180s | TesterAgent generated empty describe (0 tests) — `No test found in suite`. TS-valid, not caught by initial v1.40-a. |
| r2 | ❌ error | 44s | `llm_parse_fail` — random Gemma output issue, not Tester-related. |
| r3 | ✅ commit | 59s | 2 files. Clean. |
| bonus (after content guard) | ✅ commit | 118s | 4 files. validation_pass. Confirms content guard closes empty-describe pattern. |

**L4.1: 1/3** on first pass. **+1 bonus ✅** after content guard commit.

---

## Analysis

### What v1.40-a (TS guard) fixed
- L1.1 r2: `body is not defined` — TS error → file discarded → 3/3 ✅

### What content guard (v1.40-b) fixes
- L4.1 r1: empty describe (0 it/test) → vitest "No test found in suite" → commit_skipped
- Now caught pre-disk by regex check on content string

### Remaining noise
- L4.1 r2: `llm_parse_fail` — random model output issue, unrelated to Tester. Rate: ~1 in 6–8 runs on complex tasks.

### TesterAgent pattern after v1.40
Both failure classes from v1.39 bench are now caught:
1. TS errors (`body undefined`, wrong types) → `typeChecker.runOn` discard
2. Empty describe (0 tests) → content regex guard discard

---

## Effective sandbox result

| Suite | v1.38 | v1.39 | v1.40 |
|-------|-------|-------|-------|
| L1.1 ×3 | 3/3 | 2/3 | **3/3** ✅ |
| L4.1 ×3 | 3/3 | 2/3 | 1/3 (+ bonus ✅ post-fix) |

L4.1 `llm_parse_fail` (r2) is random — not a v1.40 regression. L4.1 with content guard: bonus run ✅.
