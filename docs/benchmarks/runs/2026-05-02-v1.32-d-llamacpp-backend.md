# Run 2026-05-02 — v1.32-d llama-swap backend swap

Phase B+D landed: rag-system теперь умеет ходить в llama-swap (OpenAI-compat
`/v1/chat/completions` + `/v1/embeddings`) вместо Ollama. Backend выбирается
через `LLM_BACKEND` env. Phase E (этот bench) валидирует parity vs Ollama
baseline; Phase F flips default.

## Configuration

| | |
|---|---|
| Date | 2026-05-02 |
| rag-system revision | `git rev-parse --short HEAD` (заполнить после commit) |
| Iteration tag | v1.32-d Phase E — backend bench |
| Backend | `LLM_BACKEND=llamacpp` |
| LLM_URL | `http://172.20.10.4:8080` |
| LLM_LARGE_MODEL | `coder` (qwen2.5-coder-32b ctx-8k) |
| LLM_SMALL_MODEL | `qwen3` (qwen3.6-35B-A3B MoE) |
| LLM_EMBED_MODEL | `embed` (nomic-embed-text-v1.5, 768 dim) |
| Tests | 426/426 unit-tests green (+34 vs v1.32-a.5: 31 backend + 3 prefix) |
| Cumulative? | no |

**Pre-bench reindex required** (embedding distribution shifts due to nomic prefixes):

```bash
rm -rf data/vectors/* data/graphs/*
# первый запуск API → автоматический re-index
```

## Tasks

### L1.1 — feature add (sandbox)

| | |
|---|---|
| Plan size | N step(s) |
| Files touched | ... |
| Validation | pass / fail / skipped |
| Commit | yes / commit_skipped |
| Wall time | Nm Ns |

**Diff highlights:**
```diff
+ ...
```

**Observations:**
- ...

---

### L4.1 — bug fix (sandbox)

| | |
|---|---|
| Plan size | N step(s) |
| Files touched | ... |
| Validation | pass / fail |
| Commit | yes / commit_skipped |
| Wall time | Nm Ns |
| Pathology fired | yes / no |

**Diff highlights:**
```diff
+ ...
```

**Observations:**
- ...

---

### scale — `rag-system-target` (×1, smoke test)

| | |
|---|---|
| Index time (cold) | Ns |
| Index time (warm cache) | Ns |
| Embedding throughput | N embed/s |
| Top-K relevance vs Ollama baseline | similar / shift detected |

## Comparison vs Ollama baseline (v1.32-a.5)

| Metric | Ollama baseline | **llama-swap** | Δ |
|---|---|---|---|
| L1.1 commit-rate | 3/3 (v1.31.1) | N/N | — |
| L4.1 healthy commit | 1/3 (a.5 small sample) | N/N | — |
| L4.1 healthy wall-time | 6 min (a.5 run #3) | N min | — |
| Tool-call recovery (inline) | active | check via logs | — |
| Pathology rate | ~33% (a.5 ×3) | N% | — |
| Network latency overhead | 0 (localhost Ollama) | Nms (172.20.10.4 vs localhost) | — |

## What worked

- ...

## What broke

- ...

## Decision

- [ ] Phase F: switch default `LLM_BACKEND=llamacpp` (если AC3-AC6 зелёные)
- [ ] Записать в ROADMAP, открыть v1.32-c (sub-agents) на новом backend
