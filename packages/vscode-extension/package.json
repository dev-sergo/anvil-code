{
  "name": "rag-system-vscode",
  "displayName": "Anvil-Code",
  "description": "Autonomous TypeScript coding agent — local LLM, no cloud. Submit tasks, watch live SSE stream, inspect results.",
  "version": "0.1.0",
  "publisher": "rag-system",
  "license": "MIT",
  "icon": "assets/icon.png",
  "repository": { "type": "git", "url": "https://github.com/bubnov-io/anvil-code.git" },
  "engines": { "vscode": "^1.85.0" },
  "categories": ["Other", "Machine Learning"],
  "keywords": ["ai", "llm", "local-ai", "typescript", "code-assistant", "rag"],
  "activationEvents": ["onStartupFinished"],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "ragSystem",
          "title": "Anvil-Code",
          "icon": "$(rocket)"
        }
      ]
    },
    "views": {
      "ragSystem": [
        { "id": "ragProjects", "name": "Projects" },
        { "id": "ragTasks", "name": "Tasks" }
      ]
    },
    "commands": [
      { "command": "rag.refresh", "title": "Anvil-Code: Refresh", "icon": "$(refresh)" },
      { "command": "rag.connect", "title": "Anvil-Code: Set API URL" },
      { "command": "rag.runTask", "title": "Anvil-Code: Submit Task", "icon": "$(play)" },
      { "command": "rag.indexProject", "title": "Anvil-Code: Index Active Project", "icon": "$(database)" },
      { "command": "rag.registerProject", "title": "Anvil-Code: Register Project", "icon": "$(add)" },
      { "command": "rag.selectActiveProject", "title": "Anvil-Code: Select Active Project" },
      { "command": "rag.streamTask", "title": "Anvil-Code: Stream Task Progress" },
      { "command": "rag.showOutput", "title": "Anvil-Code: Show Output" }
    ],
    "menus": {
      "view/title": [
        { "command": "rag.refresh", "when": "view == ragProjects", "group": "navigation" },
        { "command": "rag.refresh", "when": "view == ragTasks", "group": "navigation" },
        { "command": "rag.registerProject", "when": "view == ragProjects", "group": "navigation" },
        { "command": "rag.runTask", "when": "view == ragTasks", "group": "navigation" }
      ],
      "view/item/context": [
        { "command": "rag.streamTask", "when": "view == ragTasks && viewItem == task" },
        { "command": "rag.selectActiveProject", "when": "view == ragProjects && viewItem == project" }
      ]
    },
    "configuration": {
      "title": "Anvil-Code",
      "properties": {
        "ragSystem.apiUrl": {
          "type": "string",
          "default": "http://localhost:3000",
          "description": "Base URL of the Anvil-Code API server."
        },
        "ragSystem.refreshIntervalMs": {
          "type": "number",
          "default": 5000,
          "description": "How often to poll /tasks for status updates (set 0 to disable)."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.config.mjs",
    "watch": "node esbuild.config.mjs --watch",
    "clean": "rm -rf dist *.tsbuildinfo",
    "package": "vsce package --no-dependencies"
  },
  "dependencies": {
    "@rag-system/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/vscode": "^1.85.0",
    "esbuild": "^0.20.0",
    "typescript": "^5.4.5"
  }
}
